# What was achieved/learned
- made another UBO and learned that in order to do this, I could specify an array of set layouts in the layout create info
# Frictions
- had to backtrack on the solution of having a different ubo value, aka memcpy for different game object values. By the time the command gets submitted,
  data is already in the device and there's no way to distinguish this
# Things to try in the next session
- will need a storage descriptor for rendering different objects AND it having different positions