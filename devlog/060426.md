# What was achieved/learned
- Did some simple input with wasd to move the camera
- change the gameobjectinfo ubo to a sbo to allow for arrays of game object info to be passed
- used a push constant as an index to access a specific element in the sbo during recording of the command buffer
- allow a single mesh and its indices to be drawn to save memory IF objects are using the same mesh
# Frictions
- Holy had insane problems with the object not showing up on the screen, horrible experience
	- A mix of alot of things, winding order of vertices, vertices exceeding the near and far plane range, accidently set all the vertices in the shader's z to the same val and ignored 
	  the actual z of the object so ended up being flattened 
# Things to try in the next session
- Textures, hooray boilerplate :(