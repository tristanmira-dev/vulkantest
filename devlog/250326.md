# What was achieved/learned
- Completed the vertex buffer, (as the tutorial instructed, a vertexBuffer and a deviceMemory)
- Learned that the transfer of memory from ram due to the caching, and to the gpu is not instantaneous, however using the flag VK_MEMORY_PROPERTY_HOST_COHERENT_BIT, we dont have to flush it ourselves
- The spec guarantees that a queue submit will finish transferring the memory from the ram and graphics
- Memory in a graphics card has different types, for accessing, performance etc. before a vertex buffer could be binded, we have to check its memory requirements, and find the right index from the device's memory
- Also added a simple lil frame counter per second
# Frictions
- Had a bug as I was experimenting with the pipeline, i had the rasterizer setting as cullmode backface, and silly me because of the tutorial's structure of the
  triangles vertices(wrong winding order) the triangle did not show up so that was fun.

# Things to try in the next session
- HOPEFULLY finish staging and index buffers
- please find time to play with shaders, the thing i wanted to try using pushConstants and making the cube go back and forth, and also the pipeline settings, drawing a wireframe instead.
