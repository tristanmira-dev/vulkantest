# What was achieved/learned
- Uniform buffers fully implemented
- Simple Camera struct implemented as well

# Frictions
- Weird glm::lookAt problems when incorporating with my projection mtx. Seems like a problem with different axis conventions, either way, easier would be to just implement a simple camera later on.

# Things to try in the next session
- Multiple meshes rendered with different UBOs
	- Maybe need to add a struct for this, both the meshes and the UBOS to abstract these buffers, will see?

