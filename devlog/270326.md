# What was achieved/learned
- Implemented staging buffers
- Revised a little bit more on the buffer and device buffer nuances, and yeah just to reiterate, a buffer is a type of handle to the actual memory in the graphics card, AFTER you bind it
	- vk::MemoryRequirements memRequirements{ buffer.getMemoryRequirements() }; /*Get mem reqs of buffer, find the appropriate memory in the device*/
	  vk::MemoryAllocateInfo allocInfo{ .allocationSize = memRequirements.size, .memoryTypeIndex = findMemoryType(memRequirements.memoryTypeBits, props) /*Get the memory type that satisfies the buffer in the previous step*/};
	- For these two specifically, the buffer in the graphics card sometimes allocates padding which is why we must use the memRequirements.size() rather than the actual vertices size

# Frictions
- Of course, crashes, when i implemented the more abstract functions to make buffers and device buffers, the member vars for these were still being used instead of the actual
  params, so boom, crash because the member vars obviously start as a nullptr

# Things to try in the next session
- Staging Buffers,
- Push constants with inputs maybe? make the cube go in the direction of wasd keys. and please TRY THE SIN BOBBING BACK AND FORTH PLEASE!